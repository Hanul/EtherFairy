pragma solidity ^0.4.24;

contract FairyPayToUpgrade {

	//TODO: 돈을 지불하고 레벨업 합니다.
	//function levelUpFairy(uint256 fairyId) whenServiceRunning onlyMasterOf(fairyId) payable public {
	//}
	
	/*
	
	//TODO: 돈을 지불하고 레벨 당 증가 포인트를 올립니다.
	function increaseHPPointPerLevel(uint256 fairyIndex) whenServiceRunning onlyMasterOf(fairyId) payable public {
	}
	function increaseAttackPointPerLevel(uint256 fairyIndex) whenServiceRunning onlyMasterOf(fairyId) payable public {
	}
	function increaseDefensePointPerLevel(uint256 fairyIndex) whenServiceRunning onlyMasterOf(fairyId) payable public {
	}
	function increaseAgilityPointPerLevel(uint256 fairyIndex) whenServiceRunning onlyMasterOf(fairyId) payable public {
	}
	function increaseDexterityPointPerLevel(uint256 fairyIndex) whenServiceRunning onlyMasterOf(fairyId) payable public {
	}
	function increaseFirePointPerLevel(uint256 fairyIndex) whenServiceRunning onlyMasterOf(fairyId) payable public {
	}
	function increaseWaterPointPerLevel(uint256 fairyIndex) whenServiceRunning onlyMasterOf(fairyId) payable public {
	}
	function increaseWindPointPerLevel(uint256 fairyIndex) whenServiceRunning onlyMasterOf(fairyId) payable public {
	}
	function increaseEarthPointPerLevel(uint256 fairyIndex) whenServiceRunning onlyMasterOf(fairyId) payable public {
	}
	function increaseLightPointPerLevel(uint256 fairyIndex) whenServiceRunning onlyMasterOf(fairyId) payable public {
	}
	function increaseDarkPointPerLevel(uint256 fairyIndex) whenServiceRunning onlyMasterOf(fairyId) payable public {
	}*/
}